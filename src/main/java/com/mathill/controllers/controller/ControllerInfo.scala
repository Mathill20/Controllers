package com.mathill.controllers.controller

class ControllerInfo(val blockId: Int,
                     val metavalue: Int,
                     val maxPageCount: Int,
                     val tickFrequency: Int) {
}